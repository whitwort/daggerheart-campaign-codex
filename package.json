{
  "name": "daggerheart-campaign-codex",
  "private": true,
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase-tools": "15.28.1"
  },
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore --project daggerheart-campaign-codex-dev \"node --test tests/rules/*.test.mjs\""
  }
}
