# clasp credentials - contains OAuth tokens, must never be committed
.clasprc.json
*.clasprc.json

# Firebase service account key - contains private key, must never be committed
scripts/service-account.json
scripts/*.json
scripts/*-key.json

# Node
node_modules/

# OS/editor cruft
.DS_Store
*.swp
*~
